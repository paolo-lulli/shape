#!  /usr/bin/perl

#use lib qw(User);
use User;

save_User('$ID', '$openid', '$created', '$password', '$nickname', '$name', '$updated', '$surname');
save_User("ieria", "iiiiiun titolo di prova");

##################printall_draft();
