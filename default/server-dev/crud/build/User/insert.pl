#!  /usr/bin/perl

#use lib qw(User);
use User;

save_User('$ID', '$created', '$password', '$name', '$updated', '$surname');
save_User("ieria", "iiiiiun titolo di prova");

##################printall_draft();
